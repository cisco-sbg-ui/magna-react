---
name: Buttons**
route: /components/button
components: AButton
title: AButton
sourceCodeLink: https://github.com/cisco-sbg-ui/magna-react/tree/canary/framework/components/AButton
---

## Usage

### Import

<ComponentImport components="AButton" />

### Usage

<Playground
  withTwoPanes
  code={`<AButton
  medium
  onClick={() => alert("Hello world!")}>
  Button
</AButton>`}
/>

#### Button Variants

<Playground
  code={`<>
  <AButton medium primary className="mr-2">
    Button
  </AButton>
  <AButton medium secondary className="mr-2">
    Button
  </AButton>
  <AButton medium tertiary className="mr-2">
    Button
  </AButton>
  <AButton medium tertiaryAlt className="mr-2">
    Button
  </AButton>
  <AButton medium destructive className="mr-2">
    Button
  </AButton>
  <AButton medium destructive secondary>
    Button
  </AButton>
</>
  `}
/>

#### Sizes

`AButton` supports the small and medium size options. The `medium` prop is encouraged in the Magnetic Design System.

<Playground
  code={`<>
  <AButton small className="mr-2">
    Button
  </AButton>
  <AButton medium>
    Button
  </AButton>
</>
  `}
/>

#### With Icons

To render a button with an icon, render `<AIcon />` to either the left or right of your text, and be sure to pass the direction in which the icon sits relative to the button text to `<AIcon />`

<Playground
  code={`<div className='d-flex align-center'>
  <AButton medium className="mr-2">
    <AIcon left>pencil-simple</AIcon>
    Edit
  </AButton>
  <AButton medium secondary className="mr-2">
    Save
    <AIcon right>save</AIcon>
  </AButton>
</div>`}
/>

#### Only With An Icon

To render a button with just an icon, pass the `icon` prop to the button to indicate that there is no text to represent the button content, and then render `<AIcon />` as a direct child.

<Playground
  code={`<>
  {["primary", "secondary", "tertiary", "tertiaryAlt"].map((variant) => {
    const variantProp = {
      [variant]: true,
    };
    return (
      <AButton medium icon {...variantProp} className="mr-2" key={variant}>
        <AIcon>sync</AIcon>
      </AButton>
    );
  })}
</>
`}
/>

#### Buttons as Links

If the `href` property is used, the component will render as an HTML anchor.

<Playground
  code={`<>
  <AButton medium href="https://www.cisco.com" className="mr-2">
    Launch Cisco.com
  </AButton>
  <AButton medium href="https://www.oldschool.runescape.com" secondary>
    Launch Nostalgia
  </AButton>
</>
`}
/>

## Component Props

The `AButton` component inherits passed props.

<Props of="AButton" />

## Router Integrations

Set the `component` property to your router's link type, for example:

```jsx
import {Link} from "next/link";

<AButton component={Link} href="/">
  Home
</AButton>;
```
