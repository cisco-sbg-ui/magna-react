---
name: Headers
route: /components/header
components: AHeader, AHeaderLogo, AHeaderTitle, AHeaderNavigation, AHeaderSubtitle,
---

# Headers

## Install

Integrate with your build to [auto-import](/#integrating), or add an import in your component:

```
import {
  AHeader,
  AHeaderLogo,
  AHeaderTitle,
  AHeaderNavigation,
  AHeaderSubtitle,
} from "@cisco-sbg-ui/magna-react";
```

## Usage

<Playground
  fullWidthPreview
  code={`<AHeader>
  <AHeaderLogo href="#cisco">
    <AIcon>cisco</AIcon>
  </AHeaderLogo>
  <AHeaderTitle>
    Product Name
    <AHeaderSubtitle>Subtitle</AHeaderSubtitle>
  </AHeaderTitle>
  <AHeaderNavigation>
    <ATabGroup tall>
      <ATab selected>Demo Tab</ATab>
      <ATab href="https://www.cisco.com" target="_blank">
        Cisco.com
      </ATab>
    </ATabGroup>
  </AHeaderNavigation>
  <AButton icon tertiaryAlt href="https://www.cisco.com" target="_blank">
    <AIcon>search</AIcon>
  </AButton>
  <AButton icon tertiaryAlt href="https://www.github.com" target="_blank">
    <AIcon>github</AIcon>
  </AButton>
</AHeader>
`}
/>

## Header Props

The `AHeader` component inherits passed props.

<Props of="AHeader" />

## Header Logo Props

The `AHeaderLogo` component inherits passed props.

<Props of="AHeaderLogo" />

## Header Title Props

The `AHeaderTitle` component inherits passed props.

<Props of="AHeaderTitle" />

## Header Navigation Props

The `AHeaderNavigation` component inherits passed props.

<Props of="AHeaderNavigation" />

## Accessibility Notes

By default, `AHeader` is assigned the [TR](https://www.w3.org/TR/html51/) tag of [header](https://www.w3.org/TR/html51/sections.html#elementdef-header) which denotes that it represents introductory content for its nearest ancestor sectioning content or sectioning root element. A header typically contains a group of introductory or navigational aids.
