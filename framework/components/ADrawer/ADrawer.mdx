---
name: Drawers*
route: /components/drawer
components: ADrawer
---

# Drawers

## Install

Integrate with your build to [auto-import](/#integrating), or add an import in your component:

```
import {ADrawer} from "@cisco-sbg-ui/magna-react";
```

By default, `ADrawer` renders over the entire page as a Modal. In this sense, the document `body` is acting as the drawer's container.

You can change this behavior to have `ADrawer` render inside your own specified container.

## Usage

<Playground
  code={`() => {
    const [isLeftOpen, setIsLeftOpen] = useState(false);
    const [isRightOpen, setIsRightOpen] = useState(false);
    const [isBottomOpen, setIsBottomOpen] = useState(false);
    
    return (
      <>
        <p>Left slide-in</p>
        <AButton onClick={() => setIsLeftOpen(true)}>Open Left</AButton>
        <ADrawer isOpen={isLeftOpen}>
          <ADrawerContent>
            Left Drawer <button onClick={() => setIsLeftOpen(false)}>x</button>
          </ADrawerContent>
        </ADrawer>
        
        <p>Right slide-in</p>
        <AButton onClick={() => setIsRightOpen(true)}>Open Right</AButton>
        <ADrawer slideIn='right' isOpen={isRightOpen}>
          <ADrawerContent>
            Right Drawer <button onClick={() => setIsRightOpen(false)}>x</button>
          </ADrawerContent>
        </ADrawer>
        
        <p>Bottom slide-in</p>
        <AButton onClick={() => setIsBottomOpen(true)}>Open Bottom</AButton>
        <ADrawer slideIn='bottom' isOpen={isBottomOpen}>
          <ADrawerContent>
            Bottom Drawer <button onClick={() => setIsBottomOpen(false)}>x</button>
          </ADrawerContent>    
        </ADrawer>
      </>
    )
  }`}
/>

### Modal Options

<Playground
  code={`() => {
    const [modalOpen, setModalOpen] = useState(false);
    const [nonModalOpen, setNonModalOpen] = useState(false);
    return (
      <>
        <p>Modal Drawer</p>
        <AButton
            aria-label='Open modal drawer'
            onClick={() => setModalOpen(true)}>
          Open
        </AButton>
        <ADrawer
            asModal={true}
            isOpen={modalOpen}
            slideIn='right'>
          <MockDrawerContent onClose={() => setModalOpen(false)} />
        </ADrawer>
        
        <p>Non-Modal Drawer</p>
        <AButton
            aria-label='Open non-modal drawer'
            onClick={() => setNonModalOpen(prevState => !prevState)}>
            Toggle
        </AButton>
        <ADrawer
            asModal={false}
            isOpen={nonModalOpen}
            slideIn='right'>
          <MockDrawerContent onClose={() => setNonModalOpen(false)} />
        </ADrawer>
      </>
    )
    function MockDrawerContent({ onClose }) {
      return (
        <>
          <AListItem twoLine>
            <AListItemContent>
              <AListItemTitle><h4><b>Event Details</b></h4></AListItem>
            </AListItemContent>
            <AListItemAction>
              <AButton onClick={onClose} tertiaryAlt icon>
                <AIcon>close</AIcon>
              </AButton>
            </AListItemAction>
          </AListItem>
          <AListItem twoLine>
            <AListItemContent style={{paddingTop: '0'}}>
              <div>
                <span style={{display: 'flex', alignItems: 'center'}}>Risk Score <ATag className="status-red white--text ma-1">750</ATag></span>
                <p style={{marginTop: '5px', fontWeight: 'bold', fontSize: '24px'}}>A malicious SHA-256 targeted an endpoint</p>
              </div>
              <div>
                <p style={{margin: '0'}}>Created 2022-08-12 12:23:01.0019 UTC</p>
                <div><a href="#">AMP Unity</a></div>
              </div>
            </AListItemContent>
          </AListItem>
          <ADivider />
          <AListItem twoLine>
            <AListItemContent>
              <AListItemTitle><h4><b>Recommended Actions</b></h4></AListItem>
            </AListItemContent>
            <AListItemAction>
              <AButton tertiaryAlt>
                <AIcon>chevron-up</AIcon>
              </AButton>
            </AListItemAction>
          </AListItem>
          <AListItem twoLine>
            <AListItemContent style={{paddingTop: '0'}}>
              <APanel style={{margin: '0', padding: '5px', width: '100%'}}>
                <a href="#">Quarantine Device</a>
              </APanel>
              <APanel style={{margin: '5px 0 0 0', padding: '5px', width: '100%'}}>
                Workflow
                <br />
                <a href="#">Move to Triage Group</a>
              </APanel>
            </AListItemContent>
          </AListItem>
          <div style={{width: '100%', position: 'absolute', bottom: '0', height: '50px'}}>
            <ADivider style={{margin: 0}} />
            <AListItem twoLine>
              <AListItemContent><AButton>Add note</AButton></AListItemContent>
            </AListItem>
          </div>
        </>
      )
    }
  }`}
/>

### Self Contained Drawer

<Playground
  code={`() => {
    const [isOpen, setIsOpen] = useState(false);
    return (
      <AContainer>
        <ARow className="mb-0 grey--darken-7 white--text">
          <ACol>
            <AIcon onClick={() => setIsOpen(prevState => !prevState)} style={{cursor: 'pointer'}} size='small'>{isOpen ? 'chevron-right' : 'chevron-left'}</AIcon>
          </ACol>
          <ASpacer />
          <ACol className="d-flex align-center justify-end">
            App
          </ACol>
        </ARow>
        <ARow style={{position: 'relative', overflow: 'hidden'}}>
          <ACol cols='auto' className='pa-0'>
            <ADrawer
                className='grey--lighten-6'
                style={{maxWidth: '150px'}}
                asModal={false}
                contained
                isOpen={isOpen}>
              <AList className='grey--lighten-6' style={{border: 'none'}}>
                <AListItem twoLine href="#" target="_blank">
                  <AListItemAvatar className='mr-4 flex-shrink-0'><AIcon size={18}>chart-pie</AIcon></AListItemAvatar>
                  <AListItemContent>Foo</AListItemContent>
                </AListItem>
                <AListItem twoLine href="#" target="_blank">
                  <AListItemAvatar className='mr-4 flex-shrink-0'><AIcon size={18}>chart-curve</AIcon></AListItemAvatar>
                  <AListItemContent>Bar</AListItemContent>
                </AListItem>
                <AListItem twoLine href="#" target="_blank">
                  <AListItemAvatar className='mr-4 flex-shrink-0'><AIcon size={18}>chart-bar</AIcon></AListItemAvatar>
                  <AListItemContent>Baz</AListItemContent>
                </AListItem>
                <AListItem twoLine href="#" target="_blank">
                  <AListItemAvatar className='mr-4 flex-shrink-0'><AIcon size={18}>calendar</AIcon></AListItemAvatar>
                  <AListItemContent>Qaz</AListItemContent>
                </AListItem>
              </AList>
            </ADrawer>
          </ACol>
          <ACol className="dark-cool-grey white--text" style={{height: '300px'}}>
            <main>Main Content</main>
          </ACol>
        </ARow>
      </AContainer>
    )
  }`}
/>

### Inline Drawer

<Playground
  code={`() => {
    const [open, setOpen] = useState(false);
    const [isSlim, setIsSlim] = useState(true);
    return (
      <AContainer>
        <ARow className="mb-0 grey--darken-7 white--text">
          <ACol>
            <AIcon onClick={() => setIsSlim(prevState => !prevState)} style={{cursor: 'pointer'}} size='small'>{isSlim ? 'chevron-right' : 'chevron-left'}</AIcon>
          </ACol>
          <ASpacer />
          <ACol className="d-flex align-center justify-end">
            App
          </ACol>
        </ARow>
        <ARow>
          <ACol cols='auto' className='pa-0'>
            <ADrawer
                className='grey--lighten-6'
                style={{maxWidth: '150px'}}
                asModal={false}
                inline
                isOpen
                slim={isSlim}>
              <AList className='grey--lighten-6' style={{border: 'none'}}>
                <AListItem twoLine href="#" target="_blank">
                  <AListItemAvatar className='mr-4 flex-shrink-0'><AIcon size={18}>chart-pie</AIcon></AListItemAvatar>
                  <AListItemContent>Foo</AListItemContent>
                </AListItem>
                <AListItem twoLine href="#" target="_blank">
                  <AListItemAvatar className='mr-4 flex-shrink-0'><AIcon size={18}>chart-curve</AIcon></AListItemAvatar>
                  <AListItemContent>Bar</AListItemContent>
                </AListItem>
                <AListItem twoLine href="#" target="_blank">
                  <AListItemAvatar className='mr-4 flex-shrink-0'><AIcon size={18}>chart-bar</AIcon></AListItemAvatar>
                  <AListItemContent>Baz</AListItemContent>
                </AListItem>
                <AListItem twoLine href="#" target="_blank">
                  <AListItemAvatar className='mr-4 flex-shrink-0'><AIcon size={18}>calendar</AIcon></AListItemAvatar>
                  <AListItemContent>Qaz</AListItemContent>
                </AListItem>
              </AList>
            </ADrawer>
          </ACol>
          <ACol className="dark-cool-grey white--text" style={{height: '300px'}}>
            <main>Main Content</main>
          </ACol>
        </ARow>
      </AContainer>
    )
  }`}
/>

### Actionable Drawer

<Playground
  code={`() => {
    const [incidentDetailsOpen, setIncidentDetailsOpen] = useState(false);
    const [isSlim, setIsSlim] = useState(true);
    return (
      <AApp>
      <AContainer>
        <ARow className="mb-0 grey--darken-7 white--text">
          <ACol>
            <AIcon onClick={() => setIsSlim(prevState => !prevState)} style={{cursor: 'pointer'}} size='small'>{isSlim ? 'chevron-right' : 'chevron-left'}</AIcon>
          </ACol>
          <ASpacer />
          <ACol className="d-flex align-center justify-end">
            App
          </ACol>
        </ARow>
        <ARow style={{overflow: 'hidden'}}>
          <ACol cols='auto' className='pa-0'>
            <ADrawer
                className='grey--lighten-6'
                style={{maxWidth: '150px'}}
                asModal={false}
                inline
                isOpen
                slim={isSlim}>
              <AList className='grey--lighten-6' style={{border: 'none'}}>
                <AListItem twoLine href="#" target="_blank">
                  <AListItemAvatar className='mr-4 flex-shrink-0'><AIcon size={18}>chart-pie</AIcon></AListItemAvatar>
                  <AListItemContent>Foo</AListItemContent>
                </AListItem>
                <AListItem twoLine href="#" target="_blank">
                  <AListItemAvatar className='mr-4 flex-shrink-0'><AIcon size={18}>chart-curve</AIcon></AListItemAvatar>
                  <AListItemContent>Bar</AListItemContent>
                </AListItem>
                <AListItem twoLine href="#" target="_blank">
                  <AListItemAvatar className='mr-4 flex-shrink-0'><AIcon size={18}>chart-bar</AIcon></AListItemAvatar>
                  <AListItemContent>Baz</AListItemContent>
                </AListItem>
                <AListItem twoLine href="#" target="_blank">
                  <AListItemAvatar className='mr-4 flex-shrink-0'><AIcon size={18}>calendar</AIcon></AListItemAvatar>
                  <AListItemContent>Qaz</AListItemContent>
                </AListItem>
              </AList>
            </ADrawer>
          </ACol>
          <ACol className="dark-cool-grey white--text" style={{height: '300px'}}>
             Main Content <AButton onClick={() => setIncidentDetailsOpen(prevState => !prevState)}>More Incident Details</AButton>
          </ACol>
          <ACol cols='auto' className='pa-0' style={{position: 'relative'}}>
            <ADrawer
                className='grey--lighten-6'
                asModal={false}
                contained
                slideIn='right'
                isOpen={incidentDetailsOpen}
                style={{maxWidth: '150px'}}>
                  <MockDrawerContent onClose={() => setIncidentDetailsOpen(false)} />
                </ADrawer>
          </ACol>
        </ARow>
      </AContainer>
      </AApp>
    )
    function MockDrawerContent({ onClose }) {
      return (
        <div>
          <AListItem twoLine>
            <AListItemContent>
              <AListItemTitle><small><b>Event Details</b></small></AListItem>
            </AListItemContent>
            <AListItemAction>
              <AButton onClick={onClose} tertiaryAlt icon>
                <AIcon>close</AIcon>
              </AButton>
            </AListItemAction>
          </AListItem>
          <AListItem twoLine>
            <AListItemContent style={{paddingTop: '0'}}>
              <div>
                <span style={{display: 'flex', alignItems: 'center'}}><small>Risk Score</small> <ATag className="status-red white--text ma-1">750</ATag></span>
                <p style={{marginTop: '5px', fontWeight: 'bold', fontSize: '10px'}}>A malicious SHA-256 targeted an endpoint</p>
              </div>
              <div>
                <small style={{margin: '0'}}>Created 2022-08-12 12:23:01.0019 UTC</small>
                <div><a href="#"><small>AMP Unity</small></a></div>
              </div>
            </AListItemContent>
          </AListItem>
        </div>
      )
    }
  }`}
/>

## Props

All Footer components (`ADrawer`) inherit passed props.

### Footer Props

<Props of="ADrawer" />
