---
name: Radios
route: /components/radio
components: ARadio, ARadioGroup
title: ARadio
sourceCodeLink: https://github.com/cisco-sbg-ui/magna-react/tree/main/framework/components/ARadio
---

## Usage

### Import

<ComponentImport components="ARadio, ARadioGroup" />

### Usage

<Playground
  code={`() => {
  const [checked, setChecked] = useState(false);
  const [disabled, setDisabled] = useState(false);
  const [wrap, setWrap] = useState(false);
  return (
    <div>
      <h3>Example</h3>
      <ARadio checked={checked} disabled={disabled} wrap={wrap}>
        {LoremIpsum}
      </ARadio>
      <ADivider />
      <h3>Options</h3>
      <div className="d-flex mt-4 flex-column">
        <ACheckbox checked={checked} onClick={() => setChecked(!checked)}>
          Checked State
        </ACheckbox>
        <ACheckbox checked={disabled} onClick={() => setDisabled(!disabled)}>
          Disabled State
        </ACheckbox>
        <ACheckbox checked={wrap} onClick={() => setWrap(!wrap)}>
          Wrap Label
        </ACheckbox>
      </div>
    </div>
  );
}
`}
/>

#### States

<Playground
  code={`<div className="d-flex flex-column">
  <ARadio>Default</ARadio>
  <ARadio checked>Checked</ARadio>
  <ARadio disabled>Disabled</ARadio>
  <ARadio disabled checked>
    Disabled and Checked
  </ARadio>
  <ARadio>Very long truncated text. {LoremIpsum}</ARadio>
  <ARadio wrap>Very long wrapping text. {LoremIpsum}</ARadio>
</div>
`}
/>

#### Radio Groups

<Playground
  code={`() => {
  const options = ["One", "Two", "Three"];
  const [selectedValue, setSelectedValue] = useState(null);
  return (
    <div>
      {options.map((option, index) => (
        <ARadio
          name="myRadioGroup"
          key={\`myRadioGroup_\${index}\`}
          checked={option === selectedValue}
          onClick={() => setSelectedValue(option)}>
          {option}
        </ARadio>
      ))}
      <ADivider />
      Selected Value: {selectedValue}
    </div>
  );
}
`}
/>

#### Aligment Groups

<Playground
  code={`() => {
  const options = ["One", "Two", "Three"];
  const [selectedValue, setSelectedValue] = useState(null);
  const [selectedValue2, setSelectedValue2] = useState(null);
  return (
    <>
      <ARadioGroup>
        {options.map((option, index) => (
          <ARadio
            name="myRadioGroup"
            description={\`\${option} description\`}
            key={\`myRadioGroup_\${index}\`}
            checked={option === selectedValue}
            onClick={() => setSelectedValue(option)}>
            {option}
          </ARadio>
        ))}
      </ARadioGroup>
      <ADivider />
      <ARadioGroup horizontal>
        {options.map((option, index) => {
          return <ARadio
            name="myRadioGroup2"
            key={\`myRadioGroup2_\${index}\`}
            checked={option === selectedValue2}
            onClick={() => setSelectedValue2(option)}>
            {option}
          </ARadio>
        })}
      </ARadioGroup>
    </>
  );
}
`}
/>

#### Variants

<Playground
  code={`<div className="d-flex flex-column">
  <ARadio small description="Wizards' Guild">Mystic gloves</ARadio>
  <ARadio small description="Recipe for Disaster quest">Barrows gloves</ARadio>
  <ARadio small description="Rogues' Den minigame">Rogue gloves</ARadio>
  <ADivider />
  <ARadioGroup horizontal small>
    <ARadio small description="Recipe for Disaster (9 subquests)">Dragon gloves</ARadio>
    <ARadio small description="Dropped by Giant rock crabs">Rockshell gloves</ARadio>
  </ARadioGroup>
</div>
`}
/>

## Component Props

The `ARadio` component inherits passed props.

<Props of="ARadio" />

The `ARadioGroup` component inherits passed props.

<Props of="ARadioGroup" />

## Accessibility Notes

By default, `ARadio` components are assigned the [WAI-ARIA](https://www.w3.org/WAI/standards-guidelines/aria/) role of [radio](https://www.w3.org/TR/wai-aria/#radio).

The `ARadio` component uses the [aria-checked](https://www.w3.org/TR/wai-aria/#aria-checked) attribute to control the input state.
