---
name: Field Base
route: /extend/field-base
components: AFieldBase
---

# Field Base Component

## Install

Integrate with your build to [auto-import](/#integrating), or add an import in your component:

```
import {AFieldBase} from "@cisco-sbg-ui/magna-react";
```

## Usage

<Playground
  code={`<div style={{maxWidth: 300}}>
  <AFieldBase
    className="mb-3"
    label="Label - Normal"
    hint="This is a sample of hint text below a form field">
    [Children]
  </AFieldBase>
  <AFieldBase
    className="mb-3"
    label="Label - Warning"
    hint="This is a sample of hint text below a form field"
    validationState="warning">
    [Children]
  </AFieldBase>
  <AFieldBase
    className="mb-3"
    label="Label - Danger"
    hint="This is a sample of hint text below a form field"
    validationState="danger">
    [Children]
  </AFieldBase>

  <AFieldBase
    className="mb-3"
    label="'hints' usage example (with danger validation state)"
    error="We have an error here !"
    hints={[
        {
            content: "Simple hint, no alert."
        },
        {
            content: "Inferred danger validation.",
            hintUsesValidationState: true
        },
        {
            content: "Overridden warning validation.",
            validationStateOverride: "warning"
        },
        {
            content: "THIS HINT SHOULD BE HIDDEN !",
            hideHintOnError: true
        }
    ]}
    validationState="danger">
    [Children]
  </AFieldBase>
</div>
`}
/>

## Field Base Props

The `AFieldBase` component inherits passed props.

<Props of="AFieldBase" />
