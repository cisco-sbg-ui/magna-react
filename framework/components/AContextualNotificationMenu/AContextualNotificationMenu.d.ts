import {type AContextualNotificationMenuProps} from "./types";

declare const AContextualNotificationMenu: <C extends React.ElementType>(
  props: AContextualNotificationMenuProps<C>
) => JSX.Element;

export default AContextualNotificationMenu;
