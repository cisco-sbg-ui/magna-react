---
name: Icons
route: /components/icon
components: AIcon
title: AIcon
sourceCodeLink: https://github.com/cisco-sbg-ui/magna-react/tree/main/framework/components/AIcon
---

## Usage

### Import

<ComponentImport components="AIcon" />

### Usage

#### Sizing

<Playground
  code={`<>
  <AIcon size={16}>
    warning
  </AIcon>
  <AIcon size={32}>
    bell
  </AIcon>
  <AIcon>
    plus
  </AIcon>
</>
`}
/>

#### Status with background

<Playground
  code={`<div className="d-flex gap-3">
  <AIcon withBg>
    excellent
  </AIcon>
  <AIcon withBg>
    positive
  </AIcon>
  <AIcon withBg>
    low-warning
  </AIcon>
  <AIcon withBg>
    warning
  </AIcon>
  <AIcon withBg>
    severe-warning
  </AIcon>
  <AIcon withBg>
    negative
  </AIcon>
  <AIcon withBg>
    inactive
  </AIcon>
  <AIcon withBg>
    info
  </AIcon>
  <AIcon withBg>
    disabled
  </AIcon>
  <AIcon withBg>
    in-progress
  </AIcon>
</div>
`}
/>

## Component Props

The `AIcon` component inherits passed props.

<Props of="AIcon" />

## Gallery

<MagnaIconGallery />

## Kebab Helper

`AIcon` will check for a kebab-case name of a camelCase icon specified for convenience

<Playground
  code={`<>
  <div>Both icons render the "text-h" svg definition:</div> <br /> <br />
  camelCase
  <AIcon>textH</AIcon>
  <br /><br />
  kebab-case
  <AIcon>text-h</AIcon>
</>
`}
/>

## Accessibility Notes

By default, `AIcon` components use the [aria-label](https://www.w3.org/TR/wai-aria/#aria-label) attribute to label the icon by name. This can be overridden by setting the `label` prop on the component.
