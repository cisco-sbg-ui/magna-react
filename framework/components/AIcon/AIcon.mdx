---
name: Icons*
route: /components/icon
components: AIcon
---

# Icons

## Install

Integrate with your build to [auto-import](/#integrating), or add an import in your component:

```
import {AIcon} from "@cisco-sbg-ui/magna-react";
```

## Usage

### Atomic

<Playground
  code={`<>
  <AIcon className="status-yellow--text">warning</AIcon>
  <AIcon size={32} className="status-red--text">
    block
  </AIcon>
  <AIcon size="large">add</AIcon>
</>
`}
/>

### Magnetic

<Playground
  code={`<>
  <AIcon iconSet="magna">warning</AIcon>
  <AIcon iconSet="magna" size={32}>
    bell
  </AIcon>
  <AIcon iconSet="magna" size="large">plus</AIcon>
</>
`}
/>

## Props

The `AIcon` component inherits passed props.

<Props of="AIcon" />

## Gallery

<IconGallery />

## Magna Icon Gallery

<MagnaIconGallery />

## Accessibility Notes

By default, `AIcon` components use the [aria-label](https://www.w3.org/TR/wai-aria/#aria-label) attribute to label the icon by name. This can be overridden by setting the `label` prop on the component.
