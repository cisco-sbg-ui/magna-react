---
name: Icons
route: /components/icon
components: AIcon
title: AIcon
sourceCodeLink: https://github.com/cisco-sbg-ui/magna-react/tree/canary/framework/components/AIcon
---

## Usage

### Import

<ComponentImport components="AIcon" />

### Usage

<Playground
  code={`<>
  <AIcon>warning</AIcon>
  <AIcon size={32}>
    bell
  </AIcon>
  <AIcon size="large">plus</AIcon>
</>
`}
/>

## Component Props

The `AIcon` component inherits passed props.

<Props of="AIcon" />

## Gallery

> Any icons not in the Gallery that were previously available in Atomic will still be used if referenced. They have been removed from the Gallery since there is no direct replacement with the Phosphor icon set.

<MagnaIconGallery />

## Atomic Gallery (Legacy)

<AtomicIconGallery />

## Kebab Helper

`AIcon` will check for a kebab-case name of a camelCase icon specified for convenience

<Playground
  code={`<>
  <div>Both icons render the "text-h" svg definition:</div> <br /> <br />
  camelCase
  <AIcon>textH</AIcon>
  <br /><br />
  kebab-case
  <AIcon>text-h</AIcon>
</>
`}
/>

## Accessibility Notes

By default, `AIcon` components use the [aria-label](https://www.w3.org/TR/wai-aria/#aria-label) attribute to label the icon by name. This can be overridden by setting the `label` prop on the component.
