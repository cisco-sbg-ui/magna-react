---
name: Checkboxes**
route: /components/checkbox
components: ACheckbox
title: ACheckbox
sourceCodeLink: https://github.com/cisco-sbg-ui/magna-react/tree/canary/framework/components/ACheckbox
---

## Usage

### Import

<ComponentImport components="ACheckbox" />

### Usage

<Playground
  withTwoPanes
  code={`function Example() {
  const [checked, setChecked] = useState(true);
  return (
    <ACheckbox
      checked={checked}
      onClick={() => setChecked((prev) => !prev)}>
      Checkbox label
    </ACheckbox>
  );
}
`}
/>

#### Intedetermine State

<Playground
  code={`function Example() {
  const [checked, setChecked] = useState(true);
  return (
    <ACheckbox
      indeterminate={checked}
      checked={checked}
      onClick={() => setChecked((prev) => !prev)}>
      Checkbox label
    </ACheckbox>
  );
}
`}
/>

#### Disabled State

<Playground
  code={`function Example() {
  const [checked, setChecked] = useState(false);
  return (
    <ACheckbox
      disabled
      checked={checked}
      onClick={() => setChecked((prev) => !prev)}>
      Checkbox label
    </ACheckbox>
  );
}
`}
/>

#### Validation States

<Playground
  code={`function Example() {
  const [checked, setChecked] = useState(true);
  return (
    <>
      <ACheckbox
        validationState='warning'
        checked={checked}
        onClick={() => setChecked((prev) => !prev)}
      >
        Warning Validation State
      </ACheckbox>
      <ACheckbox
        validationState='danger'
        checked={checked}
        onClick={() => setChecked((prev) => !prev)}
      >
        Danger Validation State
      </ACheckbox>
    </>
  )
}
`}
/>

#### Sizes

`ACheckbox` supports the small and medium size options.

<Playground
  code={`function Example() {
  const [checked, setChecked] = useState(false);
  return (
    <>
      <ACheckbox checked={checked} onClick={() => setChecked((prev) => !prev)}>
        Default
      </ACheckbox>
      <ACheckbox
        checked={checked}
        onClick={() => setChecked((prev) => !prev)}
        size="small"
      >
        Small
      </ACheckbox>
      <ACheckbox
        checked={checked}
        onClick={() => setChecked((prev) => !prev)}
        size="medium"
      >
        Medium
      </ACheckbox>
    </>
  );
}
`}
/>

#### Label Wrapping Options

By default, all checkbox labels will overflow with ellipsis, i.e., with `wrap` set to `false`. However, you can choose to instead have the text wrap onto the next line by passing `true` to `wrap`.

<Playground
  code={`function Example() {
  const [checked, setChecked] = useState(true);
  return (
    <>
      <ACheckbox
        checked={checked}
        onClick={() => setChecked((prev) => !prev)}
      >
        {LoremIpsum}
      </ACheckbox>
      <ACheckbox
        wrap={true}
        checked={checked}
        onClick={() => setChecked((prev) => !prev)}
      >
        {LoremIpsum}
      </ACheckbox>
    </>
  );
}
`}
/>

#### Custom Colors

You can pass a stock color or HEX code as a custom color.

<Playground
  code={`function Example() {
  const [checked, setChecked] = useState(true);
  return [
    "rebeccapurple",
    "#d95a1a",
    "#0BB2B8",
  ].map((color) => (
    <ACheckbox
      color={color}
      checked={checked}
      onClick={() => setChecked((prev) => !prev)}
      key={color}
    >
      {color}
    </ACheckbox>
  ));
}
`}
/>

## Component Props

The `ACheckbox` component inherits passed props.

<Props of="ACheckbox" />

## Validation

### Inline Validation

<Playground
  code={`function Example() {
  const [checked, setChecked] = useState(false);
  return (
    <ACheckbox
      checked={checked}
      onClick={() => setChecked(!checked)}
      rules={[
        {
          test: (v) => !v || "We'll still find a way to email you",
          level: "warning",
        },
      ]}
    >
      Opt-out of promotional emeails
    </ACheckbox>
  );
}
`}
/>

### Required Checkbox

<Playground
  code={`function Example() {
  const [checked, setChecked] = useState(true);
  return (
    <ACheckbox
      required
      checked={checked}
      onClick={() => setChecked(!checked)}
    >
      I agree to the terms and conditions of the privacy policy
    </ACheckbox>
  );
}
`}
/>

## Accessibility Notes

By default, `ACheckbox` components are assigned the [WAI-ARIA](https://www.w3.org/WAI/standards-guidelines/aria/) role of [checkbox](https://www.w3.org/TR/wai-aria/#checkbox).

The `ACheckbox` component uses the [aria-checked](https://www.w3.org/TR/wai-aria/#aria-checked) attribute to express the input checked state.
The `ACheckbox` component uses the [aria-disabled](https://www.w3.org/TR/wai-aria/#aria-disabled) attribute to express the input disabled state.
