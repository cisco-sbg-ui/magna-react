---
name: Tooltips
route: /components/tooltip
components: ATooltip
title: ATooltip
sourceCodeLink: https://github.com/cisco-sbg-ui/magna-react/tree/main/framework/components/ATooltip
---

## Usage

### Import

<ComponentImport components="ATooltip" />

### Usage

<AAlert dismissable={false}>
  It is necessary for `ATooltip` to be a descendant of `AApp` for mounting.
</AAlert>

<br />
<br />

<AAlert dismissable={false} level="danger">
  In most cases it is easier to use ATriggerTooltip.
</AAlert>

#### Hook with delay options

<Playground
  code={`() => {
  const iconRef = useRef(null);
  const {isOpen, open, close} = useToggle();
return (

<>
  By default the openDelay is 300ms
  <br />
  <br />
  <AIcon ref={iconRef} onMouseEnter={open} onMouseLeave={close}>
    information
  </AIcon>
  <ATooltip
    anchorRef={iconRef}
    id="tooltip_usage_1"
    open={isOpen}
    placement="bottom-right"
    pointer>
    Tooltip
  </ATooltip>
</>
); } `} />

#### Manual Usage

<Playground
  code={`() => {
  const buttonRef = useRef(null);
  const titleRef = useRef(null);
  const [openButton, setOpenButton] = useState(false);
  const [openTitle, setOpenTitle] = useState(false);
  return (
    <>
      <APanel className="mt-10">
        <APanelHeader>
          <APanelTitle
            ref={titleRef}
            aria-describedby="tooltip_usage_2"
            className="text-truncate"
            onMouseEnter={() => setOpenTitle(true)}
            onMouseLeave={() => setOpenTitle(false)}>
            {LoremIpsum}
          </APanelTitle>
          <AButton
            icon
            tertiaryAlt
            ref={buttonRef}
            onMouseEnter={() => setOpenButton(true)}
            onMouseLeave={() => setOpenButton(false)}
            aria-describedby="tooltip_usage_1">
            <AIcon>information</AIcon>
          </AButton>
        </APanelHeader>
        <APanelBody>
          Hover over the title to see the full text. Hover over the icon to
          see a tooltip with pointer.
        </APanelBody>
      </APanel>
      <ATooltip
        anchorRef={titleRef}
        id="tooltip_usage_2"
        open={openTitle}
        placement="top"
        maxWidth="50%">
        {LoremIpsum}
      </ATooltip>
      <ATooltip
        anchorRef={buttonRef}
        id="tooltip_usage_1"
        open={openButton}
        placement="bottom-right"
        pointer>
        Tooltip
      </ATooltip>
    </>
  );
}
`}
/>

#### Position

<Playground
  fullWidthPreview
  code={`() => {
  const buttonRef1 = useRef(null);
  const buttonRef2 = useRef(null);
  const buttonRef3 = useRef(null);
  const buttonRef4 = useRef(null);
  const buttonRef5 = useRef(null);
  const buttonRef6 = useRef(null);
  const buttonRef7 = useRef(null);
  const buttonRef8 = useRef(null);
  const buttonRef9 = useRef(null);
  const buttonRef10 = useRef(null);
  const buttonRef11 = useRef(null);
  const buttonRef12 = useRef(null);
  const {isOpen: isOpen1, open: open1, close: close1} = useToggle(0);
  const {isOpen: isOpen2, open: open2, close: close2} = useToggle(0);
  const {isOpen: isOpen3, open: open3, close: close3} = useToggle(0);
  const {isOpen: isOpen4, open: open4, close: close4} = useToggle(0);
  const {isOpen: isOpen5, open: open5, close: close5} = useToggle(0);
  const {isOpen: isOpen6, open: open6, close: close6} = useToggle(0);
  const {isOpen: isOpen7, open: open7, close: close7} = useToggle(0);
  const {isOpen: isOpen8, open: open8, close: close8} = useToggle(0);
  const {isOpen: isOpen9, open: open9, close: close9} = useToggle(0);
  const {isOpen: isOpen10, open: open10, close: close10} = useToggle(0);
  const {isOpen: isOpen11, open: open11, close: close11} = useToggle(0);
  const {isOpen: isOpen12, open: open12, close: close12} = useToggle(0);

return (

<AContainer className="pa-6">
  <p>
    Note: Tooltips will automatically "flip" their primary axis if there is not
    enough space to place it. In these examples, expand the browser window wide
    for the "right" positioned tooltips.
  </p>
  <ARow>
    <ACol cols="2" />
    <ACol className="text-right">
      <AButton ref={buttonRef12} onMouseEnter={open12} onMouseLeave={close12}>
        Top-Left
      </AButton>
      <ATooltip anchorRef={buttonRef12} open={isOpen12} placement="top-left">
        The tooltip will be positioned to the top of the anchor, and the tooltip
        aligned to the left edge of the anchor. Content will expand to the
        right.
      </ATooltip>
    </ACol>
    <ACol className="text-center">
      <AButton ref={buttonRef1} onMouseEnter={open1} onMouseLeave={close1}>
        Top
      </AButton>
      <ATooltip anchorRef={buttonRef1} open={isOpen1} placement="top">
        The tooltip will be positioned to the top center of the anchor
      </ATooltip>
    </ACol>
    <ACol>
      <AButton ref={buttonRef2} onMouseEnter={open2} onMouseLeave={close2}>
        Top-Right
      </AButton>
      <ATooltip anchorRef={buttonRef2} open={isOpen2} placement="top-right">
        The tooltip will be positioned to the top of the anchor, and the tooltip
        aligned to the right edge of the anchor. Content will expand to the
        left.
      </ATooltip>
    </ACol>
    <ACol cols="2" />
  </ARow>
  <ARow>
    <ACol className="text-right">
      <AButton ref={buttonRef11} onMouseEnter={open11} onMouseLeave={close11}>
        Left-Top
      </AButton>
      <ATooltip anchorRef={buttonRef11} open={isOpen11} placement="left-top">
        The tooltip will be positioned to the left of the anchor, and the
        tooltip aligned to the top edge of the anchor. Content will expand to
        the bottom.
      </ATooltip>
    </ACol>
    <ACol cols="6" />
    <ACol>
      <AButton ref={buttonRef3} onMouseEnter={open3} onMouseLeave={close3}>
        Right-Top
      </AButton>
      <ATooltip anchorRef={buttonRef3} open={isOpen3} placement="right-top">
        The tooltip will be positioned to the right of the anchor, and the
        tooltip aligned to the top edge of the anchor. Content will expand to
        the bottom.
      </ATooltip>
    </ACol>
  </ARow>
  <ARow>
    <ACol className="text-right">
      <AButton ref={buttonRef10} onMouseEnter={open10} onMouseLeave={close10}>
        Left
      </AButton>
      <ATooltip anchorRef={buttonRef10} open={isOpen10} placement="left">
        The tooltip will be positioned to the left of the anchor.
      </ATooltip>
    </ACol>
    <ACol cols="6" />
    <ACol>
      <AButton ref={buttonRef4} onMouseEnter={open4} onMouseLeave={close4}>
        Right
      </AButton>
      <ATooltip anchorRef={buttonRef4} open={isOpen4} placement="right">
        The tooltip will be positioned to the right of the anchor.
      </ATooltip>
    </ACol>
  </ARow>
  <ARow>
    <ACol className="text-right">
      <AButton ref={buttonRef9} onMouseEnter={open9} onMouseLeave={close9}>
        Left-Bottom
      </AButton>
      <ATooltip anchorRef={buttonRef9} open={isOpen9} placement="left-bottom">
        The tooltip will be positioned to the left of the anchor, and the
        tooltip aligned to the bottom edge of the anchor. Content will expand to
        the top.
      </ATooltip>
    </ACol>
    <ACol cols="6" />
    <ACol>
      <AButton ref={buttonRef5} onMouseEnter={open5} onMouseLeave={close5}>
        Right-Bottom
      </AButton>
      <ATooltip anchorRef={buttonRef5} open={isOpen5} placement="right-bottom">
        The tooltip will be positioned to the right of the anchor, and the
        tooltip aligned to the bottom edge of the anchor. Content will expand to
        the top.
      </ATooltip>
    </ACol>
  </ARow>
  <ARow>
    <ACol cols="2" />
    <ACol className="text-right">
      <AButton ref={buttonRef8} onMouseEnter={open8} onMouseLeave={close8}>
        Bottom-Left
      </AButton>
      <ATooltip anchorRef={buttonRef8} open={isOpen8} placement="bottom-left">
        The tooltip will be positioned to the bottom of the anchor, and the
        tooltip aligned to the left edge of the anchor. Content will expand to
        the right.
      </ATooltip>
    </ACol>
    <ACol className="text-center">
      <AButton ref={buttonRef7} onMouseEnter={open7} onMouseLeave={close7}>
        Bottom
      </AButton>
      <ATooltip anchorRef={buttonRef7} open={isOpen7} placement="bottom">
        The tooltip will be positioned to the bottom center of the anchor.
      </ATooltip>
    </ACol>
    <ACol>
      <AButton ref={buttonRef6} onMouseEnter={open6} onMouseLeave={close6}>
        Bottom-Right
      </AButton>
      <ATooltip anchorRef={buttonRef6} open={isOpen6} placement="bottom-right">
        The tooltip will be positioned to the bottom of the anchor, and the
        tooltip aligned to the right edge of the anchor. Content will expand to
        the left.
      </ATooltip>
    </ACol>
    <ACol cols="2" />
  </ARow>
</AContainer>
); } `} />

#### Pointer

<Playground
  code={`() => {
  const buttonRef1 = useRef(null);
  const buttonRef2 = useRef(null);
  const [open1, setOpen1] = useState(false);
  const [open2, setOpen2] = useState(false);
  return (
    <>
      <p>
        If the anchor is larger than the tooltip, the pointer is placed at an offset
        to correspond to the secondary axis.
      </p>
      <p>
        If the tooltip is larger than the anchor, the pointer is placed in the
        center of the anchor.
      </p>
      <AButton secondary ref={buttonRef1} onClick={() => setOpen1(!open1)}>
        Large, Short Anchor Has Less Wide, Taller Tooltips
      </AButton>
      <ATooltip
        anchorRef={buttonRef1}
        open={open1}
        placement="bottom-left"
        pointer>
        Tooltip Less Wide Than Anchor
      </ATooltip>
      <ATooltip
        anchorRef={buttonRef1}
        open={open1}
        placement="right-top"
        pointer>
        Tooltip Taller
        <br />
        Than Anchor
      </ATooltip>
      <br />
      <AButton
        icon
        secondary
        ref={buttonRef2}
        className="mt-10 mb-6"
        onClick={() => setOpen2(!open2)}>
        <AIcon>information</AIcon>
      </AButton>
      <ATooltip
        anchorRef={buttonRef2}
        open={open2}
        placement="bottom-left"
        pointer>
        Tooltip Wider Than Anchor
      </ATooltip>
      <ATooltip
        anchorRef={buttonRef2}
        open={open2}
        placement="right-top"
        pointer>
        Tooltip Shorter Than Anchor
      </ATooltip>
      <ATooltip
        anchorRef={buttonRef2}
        open={open2}
        placement="left"
        pointer>
        Tooltip on the leftside
      </ATooltip>
      <ATooltip
        anchorRef={buttonRef2}
        open={open2}
        placement="top"
        title="Really???"
        pointer>
        <h2 style={{color: "red"}}>Optionally...</h2>Can override styles
      </ATooltip>
    </>
  );
}
`}
/>

#### `onClose` Handling

<Playground
  code={`() => {
  const buttonRef = useRef(null);
  const [open, setOpen] = useState(false);
  return (
    <AContainer fluid>
      <ARow>
        <ACol className="text-center">
          <p className="onclose-1__instruction">
            Clicking outside of the anchor or tooltip will close the tooltip
            in this example.
          </p>
          <AButton ref={buttonRef} onClick={() => setOpen(!open)}>
            Bottom
          </AButton>
          <ATooltip
            anchorRef={buttonRef}
            open={open}
            placement="bottom"
            onClose={() => setOpen(false)}>
            Tooltip
          </ATooltip>
        </ACol>
      </ARow>
    </AContainer>
  );
}
`}
/>

## Component Props

The `ATooltip` component inherits passed props.

<Props of="ATooltip" />

## Accessibility Notes

By default, `ATooltip` components are assigned the [WAI-ARIA](https://www.w3.org/WAI/standards-guidelines/aria/) role of [tooltip](https://www.w3.org/TR/wai-aria/#tooltip).

For better accessibility: add an `id` to the tooltip and set the `aria-describedby` attribute on the anchor to the new `id`.
