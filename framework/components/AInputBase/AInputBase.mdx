---
name: Input Base
route: /extend/input-base
components: AInputBase
title: AInputBase
sourceCodeLink: https://github.com/cisco-sbg-ui/magna-react/tree/main/framework/components/AInputBase
---

## Usage

### Import

<ComponentImport components="AInputBase" />

### Usage

<Playground
  withTwoPanes
  code={`
<AInputBase
  append="[Append]"
  prepend="[Prepend]"
  className="mb-3"
  clearable
  onClear={() => alert("cleared")}
  label="Label - Normal"
  hints="This is a sample of hint text below a form input (hover over the input box to see the hover effect)"
>
  [Children]
</AInputBase>
  `}
/>

#### Focused

<Playground
  code={`
<AInputBase
  append="[Append]"
  prepend="[Prepend]"
  className="mb-3"
  clearable
  onClear={() => alert("cleared")}
  label="Label - Focused"
  hints="This is a sample of hint text below a form input"
  focused
>
  [Children]
</AInputBase>
  `}
/>

#### Read Only

<Playground
  code={`
<AInputBase
  append="[Append]"
  prepend="[Prepend]"
  className="mb-3"
  clearable
  onClear={() => alert("cleared")}
  label="Label - Read-only"
  hints="This is a sample of hint text below a form input"
  readOnly
>
  [Children]
</AInputBase>
  `}
/>

#### Read Only

<Playground
  code={`
<AInputBase
  append="[Append]"
  prepend="[Prepend]"
  className="mb-3"
  clearable
  onClear={() => alert("cleared")}
  label="Label - Disabled"
  hints="This is a sample of hint text below a form input"
  disabled
>
  [Children]
</AInputBase>
  `}
/>

#### Warning

<Playground
  code={`
<AInputBase
  append="[Append]"
  prepend="[Prepend]"
  className="mb-3"
  clearable
  onClear={() => alert("cleared")}
  label="Label - Warning"
  hints="This is a sample of hint text below a form input"
  validationState="warning"
>
  [Children]
</AInputBase>
  `}
/>

#### Danger

<Playground
  code={`
<AInputBase
  append="[Append]"
  prepend="[Prepend]"
  className="mb-3"
  clearable
  onClear={() => alert("cleared")}
  label="Label - Danger"
  hints="This is a sample of hint text below a form input"
  validationState="danger"
>
  [Children]
</AInputBase>
  `}
/>

#### Sizes

<Playground
  code={`
<>
  {["small", "medium", "large"].map((size) => (
    <AInputBase
      append="[Append]"
      prepend="[Prepend]"
      className="mb-3"
      clearable
      onClear={() => alert("cleared")}
      label={size}
      hints="This is a sample of hint text below a form input"
      {...{ [size]: true }}
    >
      [Children]
    </AInputBase>
  ))}
</>
`}
/>

## Component Props

The `AInputBase` component inherits passed props.

<Props of="AInputBase" />
