---
name: Sliders
route: /components/slider
components: ASlider
title: ASlider
sourceCodeLink: https://github.com/cisco-sbg-ui/magna-react/tree/canary/framework/components/ASlider
---

## Usage

### Import

<ComponentImport components="ASlider" />

### Usage

<Playground
  fullWidthPreview
  code={`() => {
  const [value, setValue] = useState(2);
  return (
    <ASlider
      label="Likert Variant"
      value={value}
      onChange={(v) => setValue(v)}
      ticks={["Strongly Disagree", "Disagree", "Neutral", "Agree", "Strongly Agree", "N/A"]}
      hints={\`Value: \${value}\`}
      rules={[{
        test: v => [3,4].includes(v) || "You must agree"
      }]}
      max={5}
    />
  );
}
`}
/>

#### Range

<Playground
  fullWidthPreview
  code={`() => {
  const [value, setValue] = useState([0, 1000]);
  return (
    <ASlider
      label="Percentage"
      value={value}
      onChange={(v) => setValue(v)}
      ticks={[0, 250, 500, 750, 1000]}
      hints={\`Value: \${value}\`}
      max={1000}
      step={15}
    />
  );
}
`}
/>

#### States

<Playground
  code={`() => {
  const [value, setValue] = useState([25, 75]);
  return (
    <>
      <ASlider
        className="mb-6"
        label="Default Slider"
        value={value}
        onChange={(v) => setValue(v)}
        ticks={[0, 50, 100]}
        hints="Default"
      />
      <ASlider
        className="mb-6"
        label="Disabled Slider"
        value={value}
        onChange={(v) => setValue(v)}
        ticks={[0, 50, 100]}
        hints="Disabled"
        disabled
      />
      <ASlider
        className="mb-6"
        label="Warning Slider"
        value={value}
        onChange={(v) => setValue(v)}
        ticks={[0, 50, 100]}
        hints="Warning"
        validationState="warning"
      />
      <ASlider
        className="mb-6"
        label="Danger Slider"
        value={value}
        onChange={(v) => setValue(v)}
        ticks={[0, 50, 100]}
        hints="Danger"
        validationState="danger"
      />
    </>
  );
}
`}
/>

## Component Props

The `ASlider` component inherits passed props.

<Props of="ASlider" />

## Accessibility Notes

`ASlider` follows the [WAI-ARIA](https://www.w3.org/WAI/standards-guidelines/aria/) recommendations
for [slider](https://www.w3.org/TR/wai-aria/#slider), with appropriate `role`, `aria-valuemin`,
`aria-valuemax`, `aria-valuenow`, `aria-readonly` attributes on the slider thumbs.

Each slider thumb on `ASlider` has the following keyboard event handlers:

- arrow-right, arrow-up: increases slider value one step.
- arrow-left, arrow-down: decreases slider value one step.
- page-up: increase slider 10 steps.
- page-down: decrease slider 10 steps.
- home: decrease slider to minimum.
- end: increase slider to maximum.
